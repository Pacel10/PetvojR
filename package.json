{
  "name": "petvojr",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "build": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "gh-pages": "^4.0.0"
  }
}
